# mlops-lab-11


comments:
screenshots folder documents that the steps were succesfull (S3 bucket, image on ECR, and a curl request to an api)
In the integration job I added some steps that were previously only in the deployment model, they were added because in order to test the api server I needed to download model from S3.
